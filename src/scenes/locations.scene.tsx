import * as React from 'react';
import { AppLayout } from 'layouts';
import { LocationsCollectionContainer } from 'pods/locations';

export const LocationsCollectionScene = () => (
  <AppLayout>
    <LocationsCollectionContainer />
  </AppLayout>
);