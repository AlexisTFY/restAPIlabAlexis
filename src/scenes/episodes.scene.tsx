import * as React from 'react';
import { AppLayout } from 'layouts';
import { EpisodesCollectionContainer} from 'pods/episodes';

export const EpisodesCollectionScene = () => (
  <AppLayout>
    <EpisodesCollectionContainer />
  </AppLayout>
);